What should the user be able to do?
- save request
- submit request
- edit
- decide ( Approve or Reject )

What programming stack to use?
- TypeScript is prefered
- The goal is to show my capabilities

I will stick to pure JavaScript, cause switching to TypeScript will increase the efforts and switch the focus from the main goal

What should be the first endpoint?
- POST user/select because I need to pick the user first
- POST expense/select is next because I need the list
- POST expense/filter next for showing details and future search
- POST expense/insert
- POST expense/update - I make sure the actor is the creator, validate the values, update the status to open and add event
- POST expense/submit - Needs to validate the values and calculate approver
- POST expense/approve - have to validate the approver is the actor, have to add event and change the status

Time to start with the Front End!
- As a first step I will import light version of Decodela's FE framework. Even implemented in the past all of it is my creation.
- The implementations for the current project can be found in /u/js/feature directory.

Lets create the building blocks (components) first and then organize them in views.
- User entries are a good start. I imagine a colored circle with the first letter and the name next to it. They will be used in the request entries for requester and approver. They will also appear in the details and the history.

I can create other components like status mark, request entries and events, but lets switch to some FE routing and implement the user picker for the start, the route to requests list on user selection.
- in the user picker I will use two important channels for spreading data - central state and custom events. The users loaded from BE are cached in the cantral state, while user pick will fire event so any component(widget) in the UI can subscribe and act on such change, while have the central state available.

Now we have routing engine and user picker so we can continue to the requests list
- What I see in the request list entries?
	- Requester
	- Type
	- Amount
	- Approver
	- Status
	I have component for users, most of the fields are raw data
	I think only the Status needs special attention - I will create Status UI component











